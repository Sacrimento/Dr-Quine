#include <stdio.h>
#include <stdlib.h>
#define X 5
int main() {if (X < 0) {return 1;} char *s = "#include <stdio.h>%1$c#include <stdlib.h>%1$c#define X %4$d%1$cint main() {if (X < 0) {return 1;} char *s = %2$c%3$s%2$c; char p[4096]; sprintf(p, %2$cSully_%5$cd.c%2$c, X); FILE *f = fopen(p, %2$cw%2$c); if (!f) {return 1;} fprintf(f, s, 10, 34, s, X-1, 37); fclose(f); char c[4096]; sprintf(c, %2$cgcc -Wall -Werror -Wextra ./Sully_%5$c1$d.c -o ./Sully_%5$c1$d%2$c, X); system(c); sprintf(c, %2$c./Sully_%5$c1$d%2$c, X); if (X <= 0) {return 1;} system(c);}%1$c"; char p[4096]; sprintf(p, "Sully_%d.c", X); FILE *f = fopen(p, "w"); if (!f) {return 1;} fprintf(f, s, 10, 34, s, X-1, 37); fclose(f); char c[4096]; sprintf(c, "gcc -Wall -Werror -Wextra ./Sully_%1$d.c -o ./Sully_%1$d", X); system(c); sprintf(c, "./Sully_%1$d", X); if (X <= 0) {return 1;} system(c);}
